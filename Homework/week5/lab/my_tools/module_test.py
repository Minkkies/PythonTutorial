def greeting(name):
    return f"สวัสดี, {name}"
print("บรรทัดนี้จะทํางานเสมอ ไม่ว่าจะ Run หรือ Import")
# ตรวจสอบว่าไฟล์นี้ถูก Run โดยตรงหรือไม่
if __name__ == "__main__":
    print(">>> ส่วนนี้จะทํางานเฉพาะตอนที่Run ไฟล์นี้โดยตรงเท่านั้น <<<")
    # ทดสอบฟังก์ชัน
    print(greeting("สมชาย"))